# Отчёт. Шаг 5. Часы в статусе

## Что проверялось

Два тикета 10:00–16:00 при перерыве 14:00–15:00: по 18000 с сырых, вместе 36000. Лимит дня 28800, масштаб целыми секундами, каждому 14400. Задача, чей первый `active` раньше полуночи периода, отдаёт свои 14400 в inherited, вторая — в own. Параллельность 1.25, отношение к ёмкости 1.

`asOf` в 10:00 первого дня периода: прошедших рабочих дней нет, значение и параллельность `null`.

В исходниках модуля нет слов utilization и «утилизац».

## Команда

```bash
python3 -m pytest tests/test_hours.py -q
```

## Результат

3 теста, все прошли.
