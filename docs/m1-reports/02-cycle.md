# Отчёт. Шаг 2. Цикл и срок

## Что проверялось

`cycleTime` задачи C-1: короткий заход 10 минут выпал, очередь между двумя `active` в сумму не вошла, после выхода из terminal посчитаны оба длинных захода. 11:00–13:00 и 16:00–18:00 по окну 10:00–19:00 с перерывом 14:00–15:00 дают 14400 с, не 7200 до первого закрытия. Медиана 14400 / 28800. В тексте «Как считается» есть 900.

Задача только с коротким пребыванием уходит в `excluded` с причиной `shorter-than-min-stay`, значение `null`.

`waitTime` отдельно: Review 10:00–12:00 — 7200 с.

Срок: `2026-09-09T20:30:00Z` в Москве это 23:30 9 сентября, раньше due 10 сентября. `2026-09-09T21:30:00Z` — уже 10 сентября, `closedBeforeDue` ложь.

## Команда

```bash
python3 -m pytest tests/test_cycle.py -q
```

## Результат

3 теста, все прошли.
