# Отчёт. Шаг 1. Конфиг и время

## Что проверялось

`team.example.yaml` загружается, `authEnv` остаётся именем переменной. Cloud-пример без story points выключает поле SP, перерыв без `breakStart` встаёт на 13:00. Чужая `catalogVersion`, версия формулы 2, окно дня не равное `hoursPerDay` и перерыв за границей окна отклоняются. Хеш workflow меняется вместе со статусом, хеш календаря нет.

`work_seconds`: час работы 3600 с, час перерыва 0, праздник 0, суббота 0, extra workday 3600. 21:00Z (полночь Москвы) не копится, 07:00Z (10:00 Москвы) копится. Прошедший рабочий день появляется только когда `asOf` достиг `workEnd`.

## Команда

```bash
python3 -m pytest tests/test_config.py -q
```

## Результат

12 тестов, все прошли.
