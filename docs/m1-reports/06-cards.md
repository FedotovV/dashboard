# Отчёт. Шаг 6. Остальные карточки

## Что проверялось

Гигиена ставит одну причину: пустые story points раньше пустого исполнителя. Просроченный due в гигиену не попадает.

Блокеры — список в порядке просрочка, hold, пустой исполнитель. В тексте нет слова risk.

`personLoad` оставляет человека с нулём и пропускает отсутствие.

Burndown держится на `storyPointsAtAdd` (остаток 3), поздняя оценка 8 линию не двигает. Поле SP выключено: burndown `null`, в строке задачи story points `null`, не ноль из bundle.

Пустой список эпиков — предупреждение `no-epics`, значение 0, строк нет.

## Команда

```bash
python3 -m pytest tests/test_cards.py -q
```

## Результат

6 тестов, все прошли.
