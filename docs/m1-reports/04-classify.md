# Отчёт. Шаг 4. Классификация

## Что проверялось

Ромб: две головы одной глубины, побеждает `project` по `taxonomy.priority`. Цикл связей A-1 ↔ B-1 обрывается, обе задачи остаются под головой `p`. Метка `other_tech` на задаче с головой не перебивает голову.

Задача без головы — `unknown`, не техника. Ребро `Blocks` иерархией не считается.

`unknownPct` = 100 × 2 / 6, плашка `unknown-above-threshold`.

Ничья одной глубины при равном приоритете берёт меньший id родителя. Правило метки применяется только без головы.

## Команда

```bash
python3 -m pytest tests/test_classify.py -q
```

## Результат

3 теста, все прошли.
