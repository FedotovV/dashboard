# Отчёт. Шаг 6. Локальный serve

## Что проверялось

`0.0.0.0` отвергается до прослушивания, код выхода 2. Два чтения одной страницы не меняют байты слепка. В HTML нет utilization, «утилизац», `riskScore` и кнопки. `PUT` отвечает 405. Чужой id метрики — 404. `--date` без query открывает названный файл, не самый поздний.

## Команда

```bash
python3 -m pytest tests/test_serve.py -q
```

## Результат

3 теста, все прошли.
