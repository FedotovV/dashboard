# Отчёт. Шаг 0. Документ маршрута

## Что проверялось

`GET /api/sprint` копирует `asOf`, зону, команду, покрытие и спринт из файла. Подмена `compute` исключением на ответ не влияет. `GET /api/metrics/cycleTime` содержит версию, текст и параметры и не содержит `value`. Метрика экрана периода — 404. Битый файл — 422, на место слепка пустой документ не ставится. Берётся файл с самой поздней датой в имени.

## Команда

```bash
python3 -m pytest tests/test_sprint_api.py -q
```

## Результат

5 тестов, все прошли.
