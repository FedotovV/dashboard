# Отчёт. Шаг 5. Запись правок

## Что сделано

Второй `build` не меняет байты слепка и `edits.json`. Заметка «Ждём макет» остаётся в слепке. `PUT` с полем `value` отвечает 422, чужая `revision` — 409. Удачный `PUT` не переписывает слепок; страница показывает новую заметку из файла правок. Повторный `build` в тот же день отвечает кодом 4 и оставляет старые байты. Форма `POST` сохраняет заметку чужого эпика, на экране остаётся предупреждение. Без `--edits` запись отвечает 404. В HTML нет `<button>`.

## Команда

```bash
python3 -m pytest tests/test_edits_api.py -q
```

## Результат

5 тестов, все прошли.
